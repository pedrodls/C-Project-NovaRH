#include <stdlib.h>
#include "company.c"

int main()
{
    /*FILE *arq;
    char *nome, *name;

    arq = fopen("./files/empresa.txt", "a+");
    if (arq == NULL)
    {
        printf("Falha na Abertura\n");
    }
    else
    {
        //fgets(nome, sizeof(char) * 50, stdin);
         //scanf("%[^\n]", nome);
        //fprintf(arq, "%s", nome);
        while (!feof(arq))
        {
            fscanf(arq, "%s", name);
            printf("%s\n", name);
        }
        
        // printf("%s\n", nome);
    }
    fclose(arq);*/
}